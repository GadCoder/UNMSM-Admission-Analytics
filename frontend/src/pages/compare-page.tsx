import { ShellPlaceholderPage } from './shell-placeholder-page'

export function ComparePage() {
  return (
    <ShellPlaceholderPage
      title="Compare"
      description="Comparison workflows are intentionally deferred and will plug into this layout."
    />
  )
}
